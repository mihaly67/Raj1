# HANDOVER & NEXT SESSION TODO (2026. Szeptember 3.)

## ELŐZMÉNYEK ÉS JELENLEGI ÁLLAPOT
A legutóbbi session során a Jules fizikai szerveren (100.77.191.66) elindítottuk a különféle háttér- és monitorozó folyamatokat. A rendszer jelenleg három fő komponensből áll:
1. **Hardware Monitor** (`hardware_monitor.py`): Kompakt dizájn, processzormagok órajeleinek és hőmérsékletének követése. (Fut).
2. **CPU Stress Tester** (`cpu_stress_test.py`): `mprime` (Prime95) háttérbe integrált indítása QProcess segítségével, élő log kijelzéssel. (Fut).
3. **Security Center** (`security_center.py`): ClamAV, Fail2ban és Merkava Watchdog tálca ikonjai és vezérlőfelülete. A tálcaikonok és az állapotlekérdezések (szürkülés/zöldülés) alapvetően működnek a jelszómentes `sudo` (`/etc/sudoers.d/security_center`) miatt. A duplikált folyamatokat `QLocalServer`/`QLocalSocket` alapú Singleton mintával küszöböltük ki.

## JELENLEGI HIBÁK (AZONNALI TEENDŐK)
**1. A Security Center Felbontva (MEGOLDVA)**
Hiába lett megoldva a jelszómentes (`NOPASSWD`) sudo, a GUI gombjai (Start/Stop) rákattintáskor látszólag nem csinálnak semmit, vagy legalábbis nem indítják/állítják le a szolgáltatásokat. A probléma valószínűleg a PyQt5 eseménykezelő (`clicked.connect(lambda)`) + `subprocess` + `sudo bash -c` kombinációjának szintaktikai vagy futtatási limitációjából fakad MX Linux alatt.

*Javasolt Megoldás a következő iterációra:* Mélyreható hibakeresést kell végezni a gombhívásokon. Valószínűleg le kell cserélni a `subprocess.run(shell=True)` megközelítést a `QProcess` alapú aszinkron végrehajtásra, vagy be kell vezetni egy GUI alapú hibanaplót magába a Security Center-be, hogy lássuk az stdout/stderr kimeneteket a gombnyomáskor. Esetleg újra meg kell fontolni a `pkexec` vagy egy explicit root daemon (polkit policy) használatát.

**2. A Monitorok Felülvizsgálata és Egységesítése**
Jelenleg a különféle monitorok (Hardware, Security, CPU Stress) különálló ablakok/szkriptek.
*Javasolt Megoldás a következő iterációra:* Meg kell vizsgálni a három script (`hardware_monitor.py`, `security_center.py`, `cpu_stress_test.py`) esetleges összefésülését egyetlen tabos alkalmazássá (Jules Control Panel), vagy legalább az indítási és leállítási logikájuk teljes egységesítését. Továbbá a VPS-en lévő webes "Tail/SSH monitor" (ica_monitor) kapcsolatát is át kell tekinteni.

## EGYÉB INFORMÁCIÓK
* A Contabo VPS (Misi) továbbra is csak TUDÁSTÉR és RAG proxy, minden GUI-t és tesztet a Jules (100.77.191.66) fizikai szerveren kell végezni.
* Az IP-k efemerek, támaszkodj a `SERVER_KAPCSOLATOK.md` dokumentumra a VPS-en, ha elakadnál!
* A monitor hotplug bug-ot (Nvidia/X11) egy udev szabály és script kezeli. Ne nyúlj hozzá.
